
#include "Board.h"


    Board::Board()
        : width{0}
        , height{0}
    {
    
    }

    bool Board::IsLineFull(int y)
    {
        // Проверка, заполнена ли линия
        return true;
    }

    void Board::ClearLine(int y)
    {
        // Очистка заполненной линии
    }

    void Board::PlaceTetromino(const CTetromino& tetromino)
    {
        // Размещение текущей фигуры на доске
    }

    bool Board::CheckCollision(const CTetromino& tetromino)
    {
        // Проверка столкновений
        return true;
    }

    // Другие необходимые методы
